"""
Module  : indexing.chunking.ast_chunker
Purpose : AST-structure-driven chunking fallback.
          Uses parse tree topology to determine chunk boundaries when semantic
          hints are absent, ensuring every chunk is syntactically complete.
Layer   : Indexing — Embedding Preparation
"""
