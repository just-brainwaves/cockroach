"""
Module  : indexing.dependency_mapper.call_mapper
Purpose : Resolves function call sites to their definition symbols.
          Produces cross-file CALLS edges for the knowledge graph.
          Uses symbol table + type inference heuristics for interface resolution.
Layer   : Indexing — Dependency Intelligence
"""
