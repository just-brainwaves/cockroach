"""
Module  : runtime.memory_analysis.heap_analyzer
Purpose : Processes heap dumps and memory profiles from JVM and Python runtimes.
          Identifies object retention trees, GC pressure sources,
          and large off-heap allocations through structured profile parsing.
Layer   : Runtime — Memory Intelligence
"""
