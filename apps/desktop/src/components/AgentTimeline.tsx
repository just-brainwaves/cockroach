/**
 * Module  : apps.desktop.src.components.AgentTimeline
 * Purpose : Displays the sequential reasoning steps emitted by LangGraph agents during an investigation as an animated timeline.
 * Layer   : Desktop — Components
 */
