/**
 * Module  : apps.desktop.src.components.SearchBar
 * Purpose : AI semantic search bar. Queries the vector index with natural language and returns ranked code/symbol/error matches.
 * Layer   : Desktop — Components
 */
