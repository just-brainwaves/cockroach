/**
 * Module  : apps.desktop.src.components.FileExplorer
 * Purpose : Collapsible repository file tree. Highlights files with known issues, supports semantic search, emits selection events to CodeViewer.
 * Layer   : Desktop — Components
 */
