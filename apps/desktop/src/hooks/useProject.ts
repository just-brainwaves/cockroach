/**
 * Module  : apps.desktop.src.hooks.useProject
 * Purpose : TanStack Query hook for project CRUD. Abstracts API calls with optimistic updates and cache invalidation on scan completion.
 * Layer   : Desktop — Data Hooks
 */
