/**
 * Module  : apps.desktop.src.hooks.useRuntime
 * Purpose : WebSocket hook for live telemetry streaming. Manages connection lifecycle and dispatches events to runtimeStore.
 * Layer   : Desktop — Data Hooks
 */
