/**
 * Module  : apps.desktop.src.hooks.useSearch
 * Purpose : Debounced semantic search hook. Queries the vector index endpoint and returns ranked results with source locations.
 * Layer   : Desktop — Data Hooks
 */
