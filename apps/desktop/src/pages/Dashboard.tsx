/**
 * Module  : apps.desktop.src.pages.Dashboard
 * Purpose : Project overview hub. Lists indexed projects, recent investigations, health indicators, and quick-action cards for new scans.
 * Layer   : Desktop — Pages
 */
export default function Dashboard() {
  return <div>Dashboard</div>
}
