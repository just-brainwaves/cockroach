/**
 * Module  : apps.desktop.src.stores.analysisStore
 * Purpose : Zustand store — Current investigation context, streaming agent messages, ranked root cause hypotheses, and confidence scores.
 * Layer   : Desktop — State Management
 */
import { create } from 'zustand'

// TODO: define state shape and actions
export const useAnalysisStore = create(() => ({}))
