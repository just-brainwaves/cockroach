/**
 * Module  : apps.desktop.src.stores.projectStore
 * Purpose : Zustand store — Active project, file selection, scan status, and indexed repository metadata shared across Explorer components.
 * Layer   : Desktop — State Management
 */
import { create } from 'zustand'

// TODO: define state shape and actions
export const useProjectStore = create(() => ({}))
